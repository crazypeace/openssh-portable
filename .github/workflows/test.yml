name: Test Workflow

on:
  push:
    branches: [ my-10.0p1-custom ]
  workflow_dispatch:

jobs:
  test:
    name: Simple Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Test
      run: |
        echo "Workflow is working!"
        uname -a
        pwd
        ls -la
